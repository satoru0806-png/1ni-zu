<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".SpeakNoteApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SpeakNote"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpeakNote">

        <!-- IME Service -->
        <service
            android:name=".service.SpeakNoteInputMethodService"
            android:label="SpeakNote"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Permission request activity (IME can't request permissions directly) -->
        <activity
            android:name=".settings.PermissionActivity"
            android:label="SpeakNote 権限"
            android:theme="@style/Theme.SpeakNote.Transparent"
            android:exported="false" />

        <!-- Settings -->
        <activity
            android:name=".settings.SettingsActivity"
            android:label="SpeakNote 設定"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
